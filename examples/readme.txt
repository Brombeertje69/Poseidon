

# Example with modules
This example contains the following test scenarios:
    - A call to a module within another folder
    - A call to a module within the same folder
    - A situation where 2 functions have the same name in different modules (import resolve)

# Example with classes
This example contains the following test scenarios:
    - A class with methods calling functions from other classes
    - A call to a method of a class instance

# Example with ignore
This example contains the following test scenarios:
    - A call to an external function
    - A call to a private function
    - A call to a protected function
    - A public call
    - Calls from private/protected to non-private functions

# Example with instances
This example contains the following test scenarios:
    - A class that is der